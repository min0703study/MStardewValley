#include "Stdafx.h"
#include "EffectSprite.h"

/*
HRESULT EffectSprite::init(void)
{
	mBaseSprite = GDIPLUSMANAGER->clone(IMGCLASS->EffectSprite);

	for (int i = 0; i < 7; i++) {
		ImageGp* tempEffectImg = new ImageGp;
		tempEffectImg->init(getMemDc(), mBaseSprite->getFrameBitmap(i, 7));
		mVEffectAni[eEffectAniType::EAT_ROCK_CRUSH].push_back(tempEffectImg);
	}

	return S_OK;
}

void EffectSprite::release()
{
}
*/